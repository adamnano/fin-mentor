services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: cfa_training
      POSTGRES_USER: cfa_user
      POSTGRES_PASSWORD: cfa_pass
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
